// Copie ce fichier en `config.local.js` pour ton poste local.
// ⚠️ Ne pas versionner `config.local.js` (déjà ignoré par .gitignore).
window.SMARTMONITORING_CONFIG = {
  mqttUsername: "CHANGE_ME_MQTT_USER",
  mqttPassword: "CHANGE_ME_MQTT_PASS",
  adminAccessKey: "CHANGE_ME_ADMIN_KEY"
};
